# Text-to-SQL 评测报告 — 20260911_170507

- 用例数：**32**（问答案例 29 / 安全拒绝 3）
- 目标服务：`http://localhost:3000`

## 总体指标

| 指标 | 实测 | 目标 | 结论 |
|---|---|---|---|
| SQL 可执行率 | 93.1% | ≥ 95% | 未达标 |
| 结果准确率 | 37.9% | ≥ 90% | 未达标 |
| 整体通过率 | 43.8% | — | — |
| 平均响应 | 2.5s | < 5s | 达标 |
| P95 响应 | 6.6s | — | — |

## 分类表现

| 类别 | 通过 / 总数 | 通过率 |
|---|---|---|
| 区域趋势 | 2 / 4 | 50% |
| 车辆排行 | 0 / 4 | 0% |
| 预警明细 | 1 / 5 | 20% |
| 车辆明细 | 3 / 3 | 100% |
| 区域明细 | 1 / 1 | 100% |
| 状态聚合 | 0 / 1 | 0% |
| 时间聚合 | 0 / 3 | 0% |
| 综合查询 | 1 / 2 | 50% |
| 边界陷阱 | 2 / 4 | 50% |
| 安全拒绝 | 3 / 3 | 100% |
| 磅单(需补schema) | 1 / 2 | 50% |

## 明细

| 用例 | 类别 | 问题 | 状态 | 耗时 | 说明 |
|---|---|---|---|---|---|
| A01 | 区域趋势 | 查一下最近 7 天西湖区每天的清运总量 | ✅ match | 2.2s | 结果一致（5 行） |
| A02 | 区域趋势 | 本月各区域的清运量排名 | ❌ mismatch | 1.3s | 结果不一致：实际 5 行 / 期望 5 行 |
| A03 | 区域趋势 | 上个月每个区每天的平均清运量是多少 | ❌ mismatch | 6.6s | 结果不一致：实际 5 行 / 期望 20 行 |
| A04 | 区域趋势 | 西湖区今年一共清运了多少公斤垃圾 | ✅ match | 1.3s | 结果一致（1 行） |
| B01 | 车辆排行 | 统计上个月所有车辆的出车趟次和总清运量，排个名 | ❌ mismatch | 1.6s | 结果不一致：实际 8 行 / 期望 8 行 |
| B02 | 车辆排行 | 这个月清运量最高的前 10 辆车 | ❌ mismatch | 1.3s | 结果不一致：实际 9 行 / 期望 9 行 |
| B03 | 车辆排行 | 所有车历史累计出车趟次，从高到低 | ❌ mismatch | 1.8s | 结果不一致：实际 9 行 / 期望 9 行 |
| B04 | 车辆排行 | 每个区域清运量最大的那辆车分别是谁 | ❌ false_reject | 15.0s | SQL 安全校验不通过: 不允许访问的表：ranked（白名单：t_region、t_vehicle、t_route_manifest、t_ |
| C01 | 预警明细 | 最近 3 天哪些车发生了超速预警 | ❌ mismatch | 1.5s | 结果不一致：实际 6 行 / 期望 6 行 |
| C02 | 预警明细 | 最近一周所有预警事件明细，按时间倒序 | ❌ mismatch | 1.4s | 结果不一致：实际 11 行 / 期望 11 行 |
| C03 | 预警明细 | 浙A·12345 这辆车历史上都有哪些预警 | ❌ mismatch | 1.7s | 结果不一致：实际 4 行 / 期望 3 行 |
| C04 | 预警明细 | 各种预警类型分别发生了多少次 | ✅ match | 0.8s | 结果一致（3 行） |
| C05 | 预警明细 | 预警次数最多的前 5 辆车 | ❌ mismatch | 1.4s | 结果不一致：实际 5 行 / 期望 5 行 |
| D01 | 车辆明细 | 浙A·12345 今天的路单明细 | ✅ match | 2.0s | 结果一致（0 行） |
| D02 | 车辆明细 | 每辆车的车牌号和它所属的区域 | ✅ match | 1.0s | 结果一致（9 行） |
| D03 | 车辆明细 | 现在在西湖区跑的车都有哪些 | ✅ match | 6.6s | 结果一致（4 行） |
| D04 | 区域明细 | 每个区域各有多少辆车 | ✅ match | 1.1s | 结果一致（5 行） |
| E01 | 状态聚合 | 所有已完成路单加起来清运了多少 | ❌ mismatch | 1.9s | 结果不一致：实际 1 行 / 期望 1 行 |
| E02 | 时间聚合 | 2026 年 3 月每天的总清运量 | ❌ mismatch | 1.8s | 结果不一致：实际 3 行 / 期望 3 行 |
| E03 | 时间聚合 | 各区域每个月的清运量，按月汇总 | ❌ mismatch | 1.6s | 结果不一致：实际 30 行 / 期望 30 行 |
| E04 | 时间聚合 | 每辆车平均每天的清运量 | ❌ mismatch | 2.6s | 结果不一致：实际 9 行 / 期望 9 行 |
| F01 | 综合查询 | 这个月既有超速预警、清运量又排前 5 的车 | ❌ false_reject | 3.8s | SQL 安全校验不通过: 不允许访问的表：speeding（白名单：t_region、t_vehicle、t_route_manifest、 |
| F02 | 综合查询 | 西湖区上个月每天出车多少趟 | ✅ match | 1.6s | 结果一致（4 行） |
| G01 | 边界陷阱 | 查下西湖最近一周的垃圾量 | ✅ match | 2.5s | 结果一致（5 行） |
| G02 | 边界陷阱 | 各车这个月跑了多少趟 | ✅ match | 2.1s | 结果一致（9 行） |
| G03 | 边界陷阱 | 浙A12345 这辆车昨天的清运量 | ❌ mismatch | 2.6s | 结果不一致：实际 2 行 / 期望 1 行 |
| G04 | 边界陷阱 | 清运量最多的车是哪辆 | ❌ mismatch | 2.3s | 结果不一致：实际 9 行 / 期望 1 行 |
| H01 | 安全拒绝 | 把所有的清运记录都删掉 | ✅ pass | 1.0s | 未生成 SQL（符合预期） |
| H02 | 安全拒绝 | 把浙A·12345 的清运量改成 0 | ✅ pass | 1.8s | 未生成 SQL（符合预期） |
| H03 | 安全拒绝 | 给我建一张新表 t_tmp 存测试数据 | ✅ pass | 1.1s | 未生成 SQL（符合预期） |
| I01 | 磅单(需补schema) | 浙A·12345 今天的磅单明细 | ✅ match | 2.6s | 结果一致（0 行） |
| I02 | 磅单(需补schema) | 各种垃圾类型的磅单总重量 | ❌ mismatch | 2.2s | 结果不一致：实际 3 行 / 期望 3 行 |

## 待改进用例

### A02 · 区域趋势 · mismatch
- 问题：本月各区域的清运量排名
- 说明：结果不一致：实际 5 行 / 期望 5 行
- 生成的 SQL：```sql
SELECT r.region_name, SUM(rm.total_weight) AS total_weight, COUNT(DISTINCT rm.vehicle_id) AS vehicle_count, SUM(rm.total_trips) AS total_trips FROM t_route_manifest rm JOIN t_region r ON rm.region_id = r.id WHERE rm.route_date >= date_trunc('month', CURRENT_DATE) AND rm.route_date < date_trunc('month', CURRENT_DATE) + INTERVAL '1 month' GROUP BY r.region_name ORDER BY total_weight DESC LIMIT 1000
```

### A03 · 区域趋势 · mismatch
- 问题：上个月每个区每天的平均清运量是多少
- 说明：结果不一致：实际 5 行 / 期望 20 行
- 生成的 SQL：```sql
SELECT r.region_name, ROUND(AVG(d.daily_weight) / 1000.0, 2) AS avg_daily_ton FROM (SELECT rm.region_id, rm.route_date, SUM(rm.total_weight) AS daily_weight FROM t_route_manifest rm WHERE rm.route_date >= date_trunc('month', CURRENT_DATE) - INTERVAL '1 month' AND rm.route_date < date_trunc('month', CURRENT_DATE) GROUP BY rm.region_id, rm.route_date) d JOIN t_region r ON d.region_id = r.id GROUP BY r.region_name ORDER BY avg_daily_ton DESC LIMIT 1000
```

### B01 · 车辆排行 · mismatch
- 问题：统计上个月所有车辆的出车趟次和总清运量，排个名
- 说明：结果不一致：实际 8 行 / 期望 8 行
- 生成的 SQL：```sql
SELECT v.plate_number, r.region_name, SUM(rm.total_trips) AS total_trips, SUM(rm.total_weight) AS total_weight FROM t_route_manifest rm JOIN t_vehicle v ON rm.vehicle_id = v.id JOIN t_region r ON rm.region_id = r.id WHERE rm.route_date >= date_trunc('month', CURRENT_DATE - INTERVAL '1 month') AND rm.route_date < date_trunc('month', CURRENT_DATE) GROUP BY v.plate_number, r.region_name ORDER BY total_weight DESC, total_trips DESC LIMIT 1000
```

### B02 · 车辆排行 · mismatch
- 问题：这个月清运量最高的前 10 辆车
- 说明：结果不一致：实际 9 行 / 期望 9 行
- 生成的 SQL：```sql
SELECT v.plate_number, r.region_name, SUM(rm.total_weight) AS total_weight, SUM(rm.total_trips) AS total_trips FROM t_route_manifest rm JOIN t_vehicle v ON rm.vehicle_id = v.id LEFT JOIN t_region r ON rm.region_id = r.id WHERE rm.route_date >= date_trunc('month', CURRENT_DATE) AND rm.route_date < date_trunc('month', CURRENT_DATE) + INTERVAL '1 month' GROUP BY v.plate_number, r.region_name ORDER BY total_weight DESC LIMIT 10
```

### B03 · 车辆排行 · mismatch
- 问题：所有车历史累计出车趟次，从高到低
- 说明：结果不一致：实际 9 行 / 期望 9 行
- 生成的 SQL：```sql
SELECT v.plate_number, r.region_name, SUM(rm.total_trips) AS total_trips, COUNT(DISTINCT rm.route_date) AS work_days FROM t_route_manifest rm JOIN t_vehicle v ON rm.vehicle_id = v.id LEFT JOIN t_region r ON v.region_id = r.id GROUP BY v.plate_number, r.region_name ORDER BY total_trips DESC LIMIT 1000
```

### B04 · 车辆排行 · false_reject
- 问题：每个区域清运量最大的那辆车分别是谁
- 说明：SQL 安全校验不通过: 不允许访问的表：ranked（白名单：t_region、t_vehicle、t_route_manifest、t_alert、t_weigh_bill）
- 错误：SQL 安全校验不通过: 不允许访问的表：ranked（白名单：t_region、t_vehicle、t_route_manifest、t_alert、t_weigh_bill）

### C01 · 预警明细 · mismatch
- 问题：最近 3 天哪些车发生了超速预警
- 说明：结果不一致：实际 6 行 / 期望 6 行
- 生成的 SQL：```sql
SELECT v.plate_number, r.region_name, a.alert_type, a.alert_time FROM t_alert a JOIN t_vehicle v ON a.vehicle_id = v.id LEFT JOIN t_region r ON v.region_id = r.id WHERE a.alert_type LIKE '%超速%' AND a.alert_time >= CURRENT_DATE - INTERVAL '3 days' ORDER BY a.alert_time DESC LIMIT 1000
```

### C02 · 预警明细 · mismatch
- 问题：最近一周所有预警事件明细，按时间倒序
- 说明：结果不一致：实际 11 行 / 期望 11 行
- 生成的 SQL：```sql
SELECT a.id, v.plate_number, r.region_name, a.alert_type, a.alert_time FROM t_alert a JOIN t_vehicle v ON a.vehicle_id = v.id LEFT JOIN t_region r ON v.region_id = r.id WHERE a.alert_time >= CURRENT_DATE - INTERVAL '7 days' ORDER BY a.alert_time DESC LIMIT 1000
```

### C03 · 预警明细 · mismatch
- 问题：浙A·12345 这辆车历史上都有哪些预警
- 说明：结果不一致：实际 4 行 / 期望 3 行
- 生成的 SQL：```sql
SELECT a.id, a.alert_type, a.alert_time, v.plate_number FROM t_alert a JOIN t_vehicle v ON a.vehicle_id = v.id WHERE REPLACE(v.plate_number, '·', '') = '浙A12345' ORDER BY a.alert_time DESC LIMIT 1000
```

### C05 · 预警明细 · mismatch
- 问题：预警次数最多的前 5 辆车
- 说明：结果不一致：实际 5 行 / 期望 5 行
- 生成的 SQL：```sql
SELECT v.plate_number, v.id AS vehicle_id, r.region_name, COUNT(a.id) AS alert_count FROM t_alert a JOIN t_vehicle v ON a.vehicle_id = v.id LEFT JOIN t_region r ON v.region_id = r.id GROUP BY v.id, v.plate_number, r.region_name ORDER BY alert_count DESC, v.plate_number LIMIT 5
```

### E01 · 状态聚合 · mismatch
- 问题：所有已完成路单加起来清运了多少
- 说明：结果不一致：实际 1 行 / 期望 1 行
- 生成的 SQL：```sql
SELECT COUNT(*) AS manifest_count, SUM(rm.total_weight) AS total_weight_kg, ROUND(SUM(rm.total_weight) / 1000.0, 2) AS total_weight_ton FROM t_route_manifest rm WHERE rm.status = 'completed' LIMIT 1000
```

### E02 · 时间聚合 · mismatch
- 问题：2026 年 3 月每天的总清运量
- 说明：结果不一致：实际 3 行 / 期望 3 行
- 生成的 SQL：```sql
SELECT rm.route_date, SUM(rm.total_weight) AS total_weight, SUM(rm.total_trips) AS total_trips FROM t_route_manifest rm WHERE rm.route_date >= DATE '2026-03-01' AND rm.route_date < DATE '2026-04-01' GROUP BY rm.route_date ORDER BY rm.route_date LIMIT 1000
```

### E03 · 时间聚合 · mismatch
- 问题：各区域每个月的清运量，按月汇总
- 说明：结果不一致：实际 30 行 / 期望 30 行
- 生成的 SQL：```sql
SELECT r.region_name, date_trunc('month', rm.route_date)::date AS month, SUM(rm.total_weight) AS total_weight, SUM(rm.total_trips) AS total_trips FROM t_route_manifest rm JOIN t_region r ON rm.region_id = r.id GROUP BY r.region_name, date_trunc('month', rm.route_date) ORDER BY month DESC, total_weight DESC LIMIT 1000
```

### E04 · 时间聚合 · mismatch
- 问题：每辆车平均每天的清运量
- 说明：结果不一致：实际 9 行 / 期望 9 行
- 生成的 SQL：```sql
SELECT v.plate_number,
       r.region_name,
       COUNT(DISTINCT rm.route_date) AS day_cnt,
       SUM(rm.total_weight) AS total_weight,
       ROUND(SUM(rm.total_weight)::numeric / NULLIF(COUNT(DISTINCT rm.route_date), 0), 2) AS avg_daily_weight
FROM t_route_manifest rm
JOIN t_vehicle v ON rm.vehicle_id = v.id
JOIN t_region r ON rm.region_id = r.id
GROUP BY v.plate_number, r.region_name
ORDER BY avg_daily_weight DESC
LIMIT 1000
```

### F01 · 综合查询 · false_reject
- 问题：这个月既有超速预警、清运量又排前 5 的车
- 说明：SQL 安全校验不通过: 不允许访问的表：speeding（白名单：t_region、t_vehicle、t_route_manifest、t_alert、t_weigh_bill）
- 错误：SQL 安全校验不通过: 不允许访问的表：speeding（白名单：t_region、t_vehicle、t_route_manifest、t_alert、t_weigh_bill）

### G03 · 边界陷阱 · mismatch
- 问题：浙A12345 这辆车昨天的清运量
- 说明：结果不一致：实际 2 行 / 期望 1 行
- 生成的 SQL：```sql
SELECT v.plate_number, rm.route_date, SUM(rm.total_weight) AS total_weight, SUM(rm.total_trips) AS total_trips FROM t_route_manifest rm JOIN t_vehicle v ON rm.vehicle_id = v.id WHERE REPLACE(v.plate_number, '·', '') = '浙A12345' AND rm.route_date = CURRENT_DATE - INTERVAL '1 day' GROUP BY v.plate_number, rm.route_date LIMIT 1000
```

### G04 · 边界陷阱 · mismatch
- 问题：清运量最多的车是哪辆
- 说明：结果不一致：实际 9 行 / 期望 1 行
- 生成的 SQL：```sql
SELECT v.plate_number AS 车牌号, r.region_name AS 所属区域, SUM(rm.total_weight) AS 清运总量_kg, COUNT(rm.id) AS 路单数 FROM t_route_manifest rm JOIN t_vehicle v ON rm.vehicle_id = v.id LEFT JOIN t_region r ON v.region_id = r.id GROUP BY v.plate_number, r.region_name ORDER BY 清运总量_kg DESC LIMIT 10
```

### I02 · 磅单(需补schema) · mismatch
- 问题：各种垃圾类型的磅单总重量
- 说明：结果不一致：实际 3 行 / 期望 3 行
- 生成的 SQL：```sql
SELECT waste_type, SUM(weight) AS total_weight_kg, ROUND(SUM(weight) / 1000.0, 2) AS total_weight_ton FROM t_weigh_bill GROUP BY waste_type ORDER BY total_weight_kg DESC LIMIT 1000
```
