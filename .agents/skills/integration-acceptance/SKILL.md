---
name: integration-acceptance
description: 对text2sql-garbage的浏览器、SSE、模型、PostgreSQL、安全校验、导出和评测门禁执行联调验收。
---

# 联调验收

读取需求、契约、UI规格和改动。输出到 `.agents/Documents/联调验收/`，按提问、候选表检索、模型生成、SQL 拒绝/执行、流式完成/断开、表格图表、Excel、缓存、审计、schema 漂移和评测阈值记录预期、实际命令、结果及阻塞。未连接真实模型或数据库时明确标记未验证。
