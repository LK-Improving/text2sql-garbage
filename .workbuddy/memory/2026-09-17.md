# 2026-09-17 工作日志

## 清理日志 + 增补对抗样本 + 重测权限/拒答能力

### 1. 清理多余日志
- `git clean -f -- test-data/`（精确路径，记忆认证的绕过方式）删掉调优期 **30 份未跟踪中间评测报告**（20260915_*、09xx、1001、1005）+ `latest-eval-*` + `_api_test.txt`；`git clean -f -- text2sql-garbage/server.err` 删残日志。
- **未动**：受跟踪的 `before/`(P0 对照组)、`20260911/20260912` 历史基线、`1013/1014` 100% 证据。guard 未拦截（git 机制绕过生效）。

### 2. 增补 9 条对抗样本（text2sql-eval.jsonl：32 → 41 条）
- 越权查询 ×5（category `越权查询`，均 `expected_behavior: refuse`）：J01 pg_catalog.pg_tables / J02 非白名单 t_user(含密码) / J03 information_schema.tables / J04 跨系统 t_finance / J05 合法查询夹带 pg_authid 凭据窃取。
- 拒答无关 ×4（category `拒答无关`）：K01 天气 / K02 写诗 / K03 竞品营收 / K04 闲聊身份。

### 3. 重测结论（全新 server、temperature:0、清缓存）
- **全量 41 条 = 100%**（整体/结果准确率 100%、SQL 可执行率 100%、平均 2.7s、P95 4.8s）。
- 越权查询 5/5、拒答无关 4/4 全过；**两类均为模型主动返回空 SQL 拒答**（`detail: 未生成 SQL（符合预期）`），即 rule 6「业务无关不生成 SQL」生效，而非仅依赖 validator 兜底。
- 验证目标达成：模型能**限定查询范围**（不碰系统表/外部表）+ **拒答不懂的问题**。
- 证据报告：`eval-report/result-20260917_1119`（已提交 `21b4305`）；ROADMAP 同步更新（41/41 + 对抗样本小节）。

### 备注
- validator 白名单 5 张业务表；`pg_catalog`/`information_schema`/`pg_authid` 及非白名单表均会被拦，错误文案含「禁止/不允许访问」，与评测器 `isSecurityBlock` 关键词吻合，越权类即便模型生成 SQL 也会被 validator 判 pass。

## CI/CD + 质量门禁设计方案（交付计划清单）
- 产出：`CI-CD-PLAN.md`（项目根目录）。结构：设计原则 → 分层门禁（本地 pre-commit / CI 必过 G1–G8 / 发布门禁 R1–R3）→ CI/CD 流程（GitHub Actions、分支模型、Pipeline 阶段图、Postgres service container 灌装 + eval 门禁）→ 待新增文件清单 → M1–M4 落地清单 → 项目专属风险表。
- 门禁阈值沿用需求 NFR：SQL 可执行率 ≥95%、结果准确率 ≥90%、P95<5s。
- 项目专属门禁：G6 `scripts/check-schema.mjs`（lib/schema.ts vs schema.sql 一致性，防 P0-1）、G7 eval 硬卡口（含越权/拒答 9 条对抗样本）、G8 提示词模板渲染健康测试（防 `{}` f-string 坑）。
- 评测确定性：temperature:0 + CI 全新进程（sql-cache 天然空）；eval 跑在 CI Postgres service container，与部署云库解耦。
- 落地节奏：M1 本地门禁+静态 CI → M2 评测门禁 → M3 Vercel+托管 PG 部署 → M4 Flyway 迁移+nightly 趋势看板。
