# 2026-09-19

## Netlify 部署改为分支闸门模型
- 用户把 Netlify Production branch 改成 `main`。日常开发在 `master`，发版时 `git merge master` 进 `main` 再 `git push origin main` 才触发构建。
- 等价于用分支控制部署时机，自动部署保持开启但不再每次提交都 build（省构建分钟/积分）。GitHub 默认分支仍是 `master`。
- 命令（每次发版）：`git checkout main && git pull origin main && git merge master && git push origin main`。
  首次若本地/远程无 `main`：`git checkout -b main origin/main`（或 `git branch main && git push -u origin main`）。
- 已记进 MEMORY.md 部署分支模型约定。

## 左侧「会话管理」做成真功能
- 之前 Sidebar 的「历史对话」只是把当前单次会话的 turns 倒序展示，点「新建对话 / 历史项」都只弹「暂未开放」toast——是假功能。
- 本次重构为真正的多会话模型：新增 `Conversation` 类型（id/title/turns/createdAt/updatedAt）；
  `page.tsx` 的 `turns` 状态升级为 `conversations[]` + `activeId`，`ask`/重跑/多轮上下文都按 active 会话定位；
  `Sidebar` 会话列表支持**新建 / 切换 / 删除（有内容先 confirm）/ 重命名（行内 input）**；`TopBar` 面包屑显示当前会话标题。
- 持久化：`localStorage` 的 `t2s.conversations` + `t2s.activeId`，防抖 400ms 写入（避免流式输出高频写）；
  挂载时恢复，active 失效自动回落到首个会话。
-   改动 5 个文件：types.ts / icons.tsx（加 IconTrash/IconEdit）/ Sidebar.tsx / TopBar.tsx / page.tsx。
  `tsc --noEmit` 与 `next build`（Turbopack）均通过。改动只提交到 `master`，等用户 `git release`（merge master→main）才上生产。

## 清理临时备份 & 输出未实现功能清单
- 用户要求删 `D:\Study\重点项目\_wb_backup\`（上一轮 `git checkout master` 前为防今日记忆被覆盖而建的临时副本）。
  实测该目录已不存在（脚本探测返回 `ALREADY_GONE`），无需清理；注：本机 PowerShell 工具 stdout 不被回显，需写日志文件再 Read 才看得到结果。
- 产出《后续开发计划.md》（项目根目录，未提交）：盘点当前未实现功能。
  - 占位 toast（点击 = `onUnavailable`）：侧栏「我的收藏/数据看板/数据源管理」、顶栏「数据库切换/帮助中心/账号设置」、对话区「评价回答/联网搜索」。已用 Grep 交叉确认均无后端。
  - 数据资产化缺口：对话仅 localStorage（无云端同步）、无单条结果收藏、无对话分享/导出、审计 `lib/audit.flush()` 仅内存/console 未落库、无暗色模式开关。
  - 分四阶段路线：P0 体验闭环（评价/收藏/帮助）→ P1 资产化（收藏页/看板/云端同步/多库切换）→ P2 平台化（数据源管理/账号/分享/审计/联网）→ P3 增强（暗色等）。优先矩阵 P0/P1/P2/P3 已列。

## Phase 1 落地（体验闭环收尾，已提交 master 81c8de4）
- 评价回答：ChatPanel 两个 thumb 按钮从 `onUnavailable('评价回答')` 改为真实评分；`page.tsx` 加 `ratings`(turnId→up/down) + `handleRate`（再点同方向取消），localStorage `t2s.ratings` 持久化；同时 best-effort POST `/api/feedback`（追加 `logs/feedback-*.jsonl`，复用 lib/audit 文件模式，不阻塞 UI）。types.ts 加 `RatingValue`。
- 收藏单条结果：ResultPanel 顶栏加收藏星标（始终可见），`page.tsx` 加 `favorites: FavoriteItem[]` + `handleToggleFavorite`（localStorage `t2s.favorites`，含 turnId/conversationId/title/sql/summary，可切换+toast）。types.ts 加 `FavoriteItem`。「我的收藏」查看页仍是 Phase 2。
- 帮助中心：新增 `HelpModal.tsx`（使用说明/安全边界/常见问题/快捷键，Esc+点遮罩关闭），TopBar「帮助中心」按钮改为 `onOpenHelp` 打开弹层。
- 验证：`next build` 通过（tsc 干净，4 条 API 含 /api/feedback）。仅提交 master，未上生产（分支闸门）。
- 注：本机无 `IconShield`，HelpModal 用了 IconSpark/IconCheck；next build 走 PowerShell `Set-Location` + 托管 node，stdout 不回显需写 `_build.log` 再 Read。
