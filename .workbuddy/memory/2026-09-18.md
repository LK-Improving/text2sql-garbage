# 2026-09-18

## M2 评测门禁落地（text2sql-garbage）
- 实现 G7 评测硬卡口 + G8 prompt 健康单测，CI 已跑绿（run `35294408950`）。
- 新增/改：`.github/workflows/ci.yml` 加 `eval-gate` job（Postgres:16 service + seed-db 灌库 + `pnpm start` + `pnpm eval` + `pnpm eval:gate` + 上传 artifact）；`scripts/seed-db.mjs`（零依赖 pg 灌装 + 连接重试）；`scripts/check-eval.mjs`（读最新 `eval-result-*.json`，断言 execRate≥95/answerAcc≥90/p95<5）；`lib/prompts.ts`（抽 `SYSTEM_TEMPLATE`）；`lib/__tests__/prompt-health.test.ts`（G8：inputVariables 仅 `tableInfo` 且 `{{ }}` 转义正确）；route.ts 改为 import；package.json 加 `seed:db`/`eval:gate`。
- 模型 Key 经 `gh secret set` 写入仓库 secret（DEEPSEEK_API_KEY/BASE_URL/MODEL_NAME），CI 内映射到 API_KEY/BASE_URL/MODEL_NAME，不进 git。
- 实测：41 例 acc 100% / answerAcc 100% / execRate 100% / avg 1.9s / p95 2.4s，三项 NFR 全达标。G7 job 1m37s，G1–G6 49s。
- 沙箱坑复现：Git Bash 缺 coreutils（cat/head/tail/dir/ls 不可用）；提交须用 `git commit -m` 双段（heredoc 因 cat 缺失报空信息）；git push 直跑即可。CI 推送沿用 M1 的 gh credential helper + schannel 配置。

## 待办（M3 / M4）
- M3：Vercel + 托管 PG 部署（preview / production）。
- M4：Flyway 迁移、nightly 趋势看板、lint 收紧（no-explicit-any 等）、eval artifact 仅上传最新一份报告（当前 artifact 会带上 test-data/ 下历史 eval 文件，无害仅噪声）。

## M3 部署脚手架落地（Netlify + Supabase，用户改方案）
- 用户决策：弃 Vercel+Neon，改 **Netlify（国内访问更稳）+ Supabase（云 PostgreSQL）**。
- 新增/改（commit `24d1ddf`，已推 master）：`netlify.toml`（base=text2sql-garbage + `@netlify/plugin-nextjs` + NODE_VERSION=20 + `pnpm next build`）；`.github/workflows/deploy.yml`（PR→预览 `netlify deploy`；`workflow_run` CI 成功+master→生产 `--prod`；`workflow_dispatch` 手动；用 `NETLIFY_AUTH_TOKEN`/`NETLIFY_SITE_ID`）；`.github/workflows/db-setup.yml`（手动 `workflow_dispatch` 灌 Supabase，`HOSTED_DATABASE_URL` Secret + `SEED_SQL_DIR`=仓库根 test-data）；`seed-db.mjs` 增 `SEED_SQL_DIR` 覆盖 + 多级回退（ROOT/test-data→APP/test-data）；`.env.example` 重写；`README.md` 加徽章+runbook；`CI-CD-PLAN.md` 同步 2.5/3/4 + M3 落地记录。
- 关键避坑：**子目录路径坑**——Netlify 构建上下文可能拿不到仓库根 `test-data/`，故灌云库改从 GitHub Actions 全量 checkout 跑（不依赖 Netlify 构建）。Supabase 用 Transaction pooler 串（`?sslmode=require&pgbouncer=true`），`pg` 原生支持，`db.ts` 不用改。
- 外部凭证待用户设置（未代劳）：Supabase `HOSTED_DATABASE_URL`、Netlify `NETLIFY_AUTH_TOKEN`/`NETLIFY_SITE_ID`、Netlify Env `DATABASE_URL/API_KEY/BASE_URL/MODEL_NAME`；然后跑 DB Setup + push master 触发首部署。
- 校验：3 个 .mjs `node --check` 通过；3 个 workflow YAML 经 pyyaml `safe_load` 通过；主 CI 不受影响（Deploy/DB-Setup 独立，缺 Secret 只红自身）。

- 注：db-setup.yml 里 `DATABASE_URL` 用 `env:` 直接引用 `secrets.HOSTED_DATABASE_URL`，并配独立 `if: env.DATABASE_URL != ''` 步骤 + 缺失时 `::error::` 并 `exit 1`（避免把 secrets 上下文塞进 `||` 默认值的非法写法）。

## M3 用户侧已建资源 + 线上 env 交接
- **命名**：用户选定产品名 `garbageAsk`（Netlify 站点名 `garbage-ask`）。项目意为「垃圾清运 BI 智能问数助手」。
- **Supabase**：项目 ref = `jxgxypwxquepfaktlqno`（URL `https://jxgxypwxquepfaktlqno.supabase.co`）。用户给的 `sb_publishable_...` 是前端 anon key，**本项目用不上**（后端 `pg` 直连，不用 supabase-js）。
- **线上要的是 DB 连接串**：Supabase → Project Settings → Database → Connection string → Transaction pooler，形如 `postgresql://postgres.<ref>:<密码>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require`。
- **产出**：新增 `text2sql-garbage/.env.netlify`（线上环境变量，含 5 项：DATABASE_URL/API_KEY/BASE_URL/MODEL_NAME/NEXT_TELEMETRY_DISABLED），并加 `.gitignore` 规则 `.env.netlify`（commit `2d4ad7a`）。DATABASE_URL 留 `<DB密码>/<region>` 占位待用户补。
- **Netlify 构建设置待纠正**：用户截图显示 UI 里 Publish directory=`text2sql-garbage/`、Functions directory=`text2sql-garbage/netlify/functions`，会与仓库根 `netlify.toml`（base=text2sql-garbage / publish=.next / plugin-nextjs）冲突；需把 UI 这三项留空、或 Base directory 设 `text2sql-garbage`，交给 netlify.toml 管。
- 同样连接串还需 GitHub Secret `HOSTED_DATABASE_URL`（供 db-setup.yml 灌云库）。
- **Supabase 新版 UI 取连接串位置**：不在 Settings → Database（那里只有「Database password」重置 + 「Connection pooling」池大小配置），而在**顶部绿色 `Connect` 按钮**→ 弹窗里选 Transaction pooler / URI 复制。密码创建后不可见，忘了就点 Reset database password。
- **灌数据到 Supabase**：本地数据来自 test-data/seed.sql（确定性）。两种方式：(A) 设 GitHub Secret `HOSTED_DATABASE_URL`=Transaction pooler 串 → Actions → DB Setup → Run workflow；(B) 本机 `DATABASE_URL=<Supabase串> pnpm seed:db`。脚本 DROP+CREATE 幂等，结果与本地一致。若本地还有自定义手写数据需整库搬，用 `pg_dump 本地 | psql Supabase直连串`。
