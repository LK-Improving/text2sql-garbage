# 2026-09-16 工作日志

## 评测准确率调优收尾 + 提交（P3）

目标：把结果准确率从 P1 基线 65.5% 拉到量化 ≥90%，并固化所有未提交的调优改动。

### 最终评测结论（temperature:0 + 清缓存、全新 server 端口 3210）
- 两次连续独立评测均为 **100%（32/32）**：整体通过率 100%、结果准确率 100%、SQL 可执行率 100%、平均 1.6–2.2s、P95 ≤5s。
- 之前几次摆动（93.1%–96.9%）源于少量歧义用例在 temp:0 下仍因提示词微扰翻转，已全部钉死。

### 本轮落地的改动（route.ts / jsonl / run-eval.mjs，此前均未提交）
- `route.ts`：rule 8 重写（按日二维聚合，禁止折叠日期维度）+ 例外「平均每天/每月=度量要折叠日期」；rule 9 车牌 `=` 精确匹配（去反引号）；few-shot 由 5 → **9 例**（新增 A03 按日聚合置顶、E04 平均每天折叠、F01「既…又…=交集」、G01 时间范围按天拆解）；`temperature: 0.1 → 0`。
- `text2sql-eval.jsonl`：E04 整数除法补 `* 1.0`（PG 整除截断）；E03 月份标签格式差异移出 `expected_columns`（只校验 区域+总量关键列，容忍 '2026-01' vs 2026-01-01 展示差异）；F01 改为「Top5 ∩ 本月超速」交集语义（4 行）。
- `run-eval.mjs`：`compareRows` 数值容差 0.01 + 按 `expected_columns` 列值多重集合匹配（容忍中文别名/额外列）。

### 本次新沉淀的关键坑（已写入 MEMORY.md 第 12 条）
1. 评测可复现三要素：`temperature: 0` + 评测前**重启 server 清内存缓存** + 数值容差比对器。否则命中 `@/lib/sql-cache` 计划缓存（评测均耗时 ~0.2s 即 cache hit）+ LLM 非确定性，会让一次 96.6% 重跑跌破 90%。
2. PostgreSQL 整数除法陷阱：`SUM(...) / COUNT(...)` 在整数列上整除截断（27935/16=1745 而非 1745.94），标准答案必须 `* 1.0` 强制浮点。
3. few-shot 首位优先（primacy）：模型对「每个 X 每天 Y」有强先验（CTE 折叠日期维度），规则与追加示例都被忽略，须把正确范式示例放首位。

### 提交
- 提交范围：route.ts + run-eval.mjs + text2sql-eval.jsonl（3 个源改动）+ 最终 100% 证据报告（eval-report/result-20260916_1013、1014）。
- 未提交噪音（留盘不提交）：大量中间评测报告（20260915_*、0933/0934/0954/1001/1005 等）、`_api_test.txt`、`server.err`、`latest-eval-report.md`、`latest-eval-result.json`。
- 同步更新 ROADMAP.md：准确率基线 65.5% → 100%，新增「P3 · 准确率调优」节。
